import {sfx} from '../js/audio.js';
export function showResult(host,{title,message,coins=0,win=true,onContinue}){const o=document.createElement('div');o.className='result-overlay show';o.innerHTML=`<div class="result-card"><div style="font-size:60px">${win?'🏆':'💥'}</div><h2>${title}</h2><p>${message}</p>${coins?`<div style="font-size:28px;color:#62f0cf;font-weight:1000">+${coins} COINS</div>`:''}<button class="primary">CONTINUE</button></div>`;host.querySelector('.game-shell')?.append(o);sfx(win?'cheer':'bad');o.querySelector('button').onclick=()=>{o.remove();onContinue?.()}}
