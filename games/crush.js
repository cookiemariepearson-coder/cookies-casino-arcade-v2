import {reward} from '../js/coins.js';import {sfx} from '../js/audio.js';import {showResult} from './result.js';const icons=['🍪','🍩','🧁','🍭','🍫','🍬'];let board=[],score=0,moves=22,selected=null,locked=false;export function mount(h){board=Array.from({length:64},()=>icons[Math.floor(Math.random()*icons.length)]);score=0;moves=22;selected=null;locked=false;draw(h)}function draw(h){h.innerHTML=`<div class="game-shell"><div class="game-hud"><div class="hud-box">Score: <b>${score}</b></div><div class="hud-box">Moves: <b>${moves}</b></div><div class="hud-box">Goal: 900</div></div><div class="action-feed">Tap two neighboring treats to swap.</div><div id="b" class="crush-board"></div></div>`;const g=h.querySelector('#b');board.forEach((x,i)=>{const b=document.createElement('button');b.className='crush-tile cell';b.textContent=x;b.disabled=locked;b.onclick=()=>pick(h,i);g.append(b)})}function pick(h,i){if(locked)return;if(selected===null){selected=i;return}const near=Math.abs(selected-i)===1||Math.abs(selected-i)===8;if(!near){selected=i;return}locked=true;[board[selected],board[i]]=[board[i],board[selected]];selected=null;moves--;let gain=0;for(let r=0;r<8;r++)for(let c=0;c<6;c++){const p=r*8+c;if(board[p]===board[p+1]&&board[p]===board[p+2])gain+=80}score+=gain||15;sfx(gain?'win':'card');draw(h);setTimeout(()=>{locked=false;if(score>=900){reward('crush',200);showResult(h,{title:'Cookie Crush Win!',message:'Sweet board cleared!',coins:200,onContinue:()=>mount(h)})}else if(moves<=0)showResult(h,{title:'Out of Moves',message:'This round is over.',win:false,onContinue:()=>mount(h)});else draw(h)},650)}export function unmount(){locked=true}
