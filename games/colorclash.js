import {sfx} from '../js/audio.js';import {reward} from '../js/coins.js';import {showResult} from './result.js';const colors=['#e93d4c','#287cff','#39b95a','#f0c52c'];let hand=[],top=null,bot=[],busy=false;function card(){return{color:colors[Math.floor(Math.random()*4)],n:Math.floor(Math.random()*10)}}export function mount(h){hand=Array.from({length:7},card);bot=Array.from({length:7},card);top=card();busy=false;draw(h,'Your turn. Match color or number.')}function backs(n){return `<div class="hidden-hand">${Array.from({length:n},()=>'<div class="card-back-mini"></div>').join('')}</div>`}function draw(h,m){h.innerHTML=`<div class="game-shell"><div class="turn-banner ${busy?'bot':''}">${m}</div><div class="color-arena"><div class="opponent-panel ${bot.length===1?'opponent-last':''}"><h3>Opponent</h3>${backs(bot.length)}<p>${bot.length===1?'LAST CARD!':bot.length+' cards'}</p></div><div><div class="card-row"><div class="play-card" style="background:${top.color};color:white">${top.n}</div></div></div><div class="opponent-panel"><h3>Cookie</h3><p>${hand.length} cards</p><button id="draw" class="secondary">DRAW CARD</button></div></div><h3>Your Hand</h3><div id="cards" class="card-row"></div></div>`;const r=h.querySelector('#cards');hand.forEach((c,i)=>{const b=document.createElement('button');b.className='play-card selectable';b.style.background=c.color;b.style.color='white';b.textContent=c.n;b.disabled=busy;b.onclick=()=>play(h,i);r.append(b)});h.querySelector('#draw').onclick=()=>{if(busy)return;hand.push(card());busy=true;draw(h,'You pulled one card. Opponent is playing…');setTimeout(()=>botTurn(h),1500)}}function play(h,i){if(busy)return;const c=hand[i];if(c.color!==top.color&&c.n!==top.n){sfx('bad');draw(h,'That card does not match. Pull a card or choose another.');return}top=c;hand.splice(i,1);sfx('card');if(!hand.length){reward('colorclash',220);showResult(h,{title:'Color Clash Win!',message:'You played every card first.',coins:220,onContinue:()=>mount(h)});return}busy=true;draw(h,'Opponent is playing…');setTimeout(()=>botTurn(h),1500)}function botTurn(h){const i=bot.findIndex(c=>c.color===top.color||c.n===top.n);if(i>=0){top=bot.splice(i,1)[0];sfx('card')}else bot.push(card());if(!bot.length){showResult(h,{title:'Opponent Wins',message:'You lost this round.',win:false,onContinue:()=>mount(h)});return}busy=false;draw(h,bot.length===1?'Opponent has one card left! Your turn.':'Your turn.')}export function unmount(){busy=true}
